# Snapshot report for `tests/tests-gen/formatter.test.js`

The actual snapshot is saved in `formatter.test.js.snap`.

Generated by [AVA](https://avajs.dev).

## let+if+begin

> Snapshot 1

    `(let ((x 10))␊
      (if (zero? x)␊
          (begin␊
            (display "zero")␊
            (newline))␊
          (begin␊
            (display "not zero")␊
            (newline))))`

## named let

> Snapshot 1

    `(let iter ((x 10))␊
      (if (not (zero? x))␊
          (iter (- x 1))))`

## let

> Snapshot 1

    `(let ((x 10))␊
      (print x)␊
      (print x))`

> Snapshot 2

    `(let ((x))␊
      10␊
      20␊
      "xx␊
       yy"␊
      (print x)␊
      (print x))`

> Snapshot 3

    `(let ((bar (foo bar (baz)))␊
          (foo 10))␊
      10␊
      20␊
      "xx␊
       yy"␊
      (foo bar)␊
      (print0))`

> Snapshot 4

    `(let ((bar)␊
          (foo (baz quux))␊
          (foo 10))␊
      (foo bar)␊
      (print0))`

> Snapshot 5

    `(let ((foo 10)␊
          (xxx (if (null? rest)␊
                   (current-input-port)␊
                   (car rest)))␊
          (bar foo))␊
      (foo bar)␊
      (print0))`

## cond

> Snapshot 1

    `(cond ((foo 10)␊
           10)␊
          ((foo (bar baz))␊
           (begin␊
             (display "x")␊
             (newline))␊
           ((foo)␊
            (foo bar))))`

> Snapshot 2

    `(cond ((foo 10)␊
           10)␊
          ((foo)␊
           (begin␊
             (display "x")␊
             (newline)))␊
          ((foo)␊
           (foo bar)))`

## define

> Snapshot 1

    `(define (foo x)␊
      (+ x x))`

> Snapshot 2

    `(define (foo x)␊
      "xxx"␊
      (+ x x))`

> Snapshot 3

    `(define foo (lambda (x)␊
                  (+ x x)))`

> Snapshot 4

    `(define foo (lambda (x)␊
                  "xxx" (+ x x)))`

> Snapshot 5

    '(define foo (list 1 2 3))'

> Snapshot 6

    `(define (foo x)␊
      "foo␊
       bar"␊
      (+ x x))`

> Snapshot 7

    '(define foo 10)'

## syntax-rules

> Snapshot 1

    `(syntax-rules ()␊
      ((_ x)␊
       (list x)))`

> Snapshot 2

    `(syntax-rules ()␊
      ((_)␊
       (if))␊
      ((_ x)␊
       (list x)))`

> Snapshot 3

    `(syntax-rules (==>)␊
      ((_ a ==> b)␊
       (lambda (x)␊
         (and (< x a)␊
              (> x b)))))`

## define-syntax

> Snapshot 1

    `(define-syntax foo␊
      (syntax-rules ()␊
        ((_ x)␊
         (list x))))`

## nested list

> Snapshot 1

    `((1 2 3)␊
     (1 2 3)␊
     (1 2 3))`

## string

> Snapshot 1

    `;; bar␊
    (define i 0) ;; foo␊
    (define i (let ((i 2))␊
                (+ i 3)))`
